# Simple Shell scripts I've written

## getip.sh 
Get the local and public IP your machine is using. 
To run open your terminal and run:

```shell
curl "https://raw.githubusercontent.com/cjerrington/shell/master/info.sh" -s | sh
```
